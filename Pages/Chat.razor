@page "/chathub"

@using Microsoft.AspNetCore.SignalR.Client
@using BlazorServerApp_Chess.Data

@using DataAccessLibrary.Models;
@using BlazorServerApp_Chess.Services;

@inject NavigationManager NavigationManager
@inject Blazored.SessionStorage.ISessionStorageService sessionStorage
@inject UserService UserService

@implements IDisposable


<div>
    <label>
        <a>@currentUserName</a>
    </label>
</div>
<div class="form-group">
    <label>
        Message:
        <input @bind="messageInput" size="50" />
    </label>
</div>
<button @onclick="@(() => UserService.Send("hesus", messageInput))" disabled="@(!UserService.IsConnected)">Send</button>

<p>@usersConnectedCount users online!</p>
<hr>

<ul id="messagesList">
    @foreach (var message in messages)
    {
        <li>@message</li>
    }
</ul>

@code {
    private HubConnection hubConnection;
    private List<string> messages = new List<string>();
    private string messageInput;
    private string currentUserName;
    private List<UserModel> users;
    private int usersConnectedCount = 0;


    protected override void OnInitialized()
    {

        //hubConnection = new HubConnectionBuilder()
        //    .WithUrl(NavigationManager.ToAbsoluteUri("/chathub"))
        //    .Build();
        //
        //hubConnection.On<string, string, string>("ReceiveMessage", (user, message, time) =>
        //{
        //    var encodedMsg = $"{time} >> {user} > {message}";
        //    messages.Add(encodedMsg);
        //    StateHasChanged();
        //});
        //
        //await hubConnection.StartAsync();
        //
        //currentUserName = await sessionStorage.GetItemAsync<string>("userName");
        //
        //await hubConnection.SendAsync("SendMessageToOthers", currentUserName);
        //usersConnectedCount = BlazorServerApp_Chess.Hubs.ChatHub.connectionIds.Count;

        UserService.Notify += OnNotify;

    }

    public void OnNotify()
    {
        if (!string.IsNullOrEmpty(UserService.User))
        {
            var encodedMsg = UserService.User + " says " +
                                                        UserService.Message;
            messages.Add(encodedMsg);
        }

        InvokeAsync(() =>
        {
            StateHasChanged();
        });

    }


    Task Send() =>
    hubConnection.SendAsync("SendMessage", currentUserName, messageInput, DateTime.Now.ToShortTimeString());


    public bool IsConnected =>
        hubConnection.State == HubConnectionState.Connected;


    public void Dispose()
    {
        hubConnection.SendAsync("SendMessage", currentUserName, "left the chat!", DateTime.Now.ToShortTimeString());
        _ = hubConnection.DisposeAsync();
    }
}